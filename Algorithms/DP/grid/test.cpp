#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> adj(10);
vector<int> dist(10);

void edge(int a, int b) {
    adj[a].push_back(b);
}

void bfs(int src) {
    queue<int> q;

}

int main() {
}
