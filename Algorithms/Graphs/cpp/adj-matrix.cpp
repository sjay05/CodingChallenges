// C++ Graph Adjacency Matrix Representation
#include <iostream>
using namespace std;

int adj_matrix() {
    int N = 4; // n = vertices
    int adj[N][N]; // adj[a][b] indicates if the graph contains an edge from Va to Vb, with w being stored.
}
